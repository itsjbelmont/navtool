# Development Quickstart

This project uses python for the underlying control utility.

## Activate the virtual env

### If the venv is already set up:
From the `navtool/` root directory: `source .venv/bin/activate`