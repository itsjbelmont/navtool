# NavTool

NavTool is a command line utility for efficiently navigating the filesystem between commonly accessed pre-known directory paths.

## System Requirements

* `Python3`
  * Run `python3 --version` to validate that python3 is installed

